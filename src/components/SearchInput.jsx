import React, { useContext } from 'react'
import { InputInfo } from './Header'

const SearchInput = () => {
   const {info}  = useContext(InputInfo);
    return (
        <div>
            
        </div>
    )
}

export default SearchInput
